# Supabase setup

1. Create the Supabase project named `fashion-factory-order`.
2. Apply `migrations/0001_create_orders.sql`.
3. Add the generated project URL and service-role key to Vercel environment variables.
4. Keep the service-role key server-only; never expose it through a `NEXT_PUBLIC_` variable.
